CREATE DATABASE firstapi;

CREATE TABLE users(
    id SERIAL PRIMARY KEY,
    name VARCHAR(240),
    email TEXT
);

CREATE TABLE users(
    id SERIAL PRIMARY KEY,
    name VARCHAR(240),
    email TEXT
);

INSERT INTO users(name, email) VALUES
    ('Eva', 'eva@contato.com'),
    ('Vee', 'vee@contato.com')

